{
  "name": "forge",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npx esbuild src/main.ts --bundle --outfile=dist/main.js --platform=browser --format=esm --loader:.png=dataurl --watch"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "typescript": "^5.9.3"
  },
  "devDependencies": {
    "esbuild": "^0.25.10"
  }
}
